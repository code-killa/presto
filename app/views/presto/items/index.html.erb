<% content_for :content_header do %>
  <h1> <%= @klass.to_s.capitalize %> <small>Control panel</small> </h1>
<% end %>
<div class="box">
  <!-- Default box contents -->
  <div class="box-header with-border"> <%= @klass.to_s %> list </div>
  <div class="box-body">
    <div class="row">
      <div class="col-lg-3">
        <%= link_to "create new", new_item_path, class: "btn btn-primary" %>
      </div>
    </div>
    <p>
    </p>
    <table class="table">
      <thead>
        <tr>
          <% @klass.column_names.each do |name|  %>
            <th><%= name %></th>
          <% end %>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <%= render partial: "item", collection: @item_list, as: :model, locals: {klass: @klass} %>
      </tbody>
    </table>
    <%= will_paginate @item_list, renderer: ::BootstrapPagination::Rails %>
  </div>

</div>
<ul>
</ul>

